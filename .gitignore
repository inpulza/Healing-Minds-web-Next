node_modules
.next
dist
.env
.env.*
!.env.example
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Secrets / credentials — NEVER commit (keep on disk for local reference only)
attached_assets/*credenciales*
attached_assets/*credentials*
attached_assets/*GSC*
attached_assets/*OAuth*
attached_assets/*oauth*
attached_assets/*secret*
attached_assets/*refresh*token*
*credenciales*
*refresh-token*

# Volcados de auditoría de crawl (artefactos locales, no versionados)
attached_assets/healing-minds-crawl_*.json
attached_assets/healing-minds-googlebot-crawl_*.json
attached_assets/healing-minds-rendered_*.json

# Registro local de assets del agente: apunta a ficheros de este entorno
# (intermedios sin versionar), así que versionarlo deja referencias muertas.
.agents/agent_assets_metadata.toml
.vercel
.env*

# Local capture/render output: keep summaries, not hundreds of generated PNGs.
_arnes/evidencia/visual/*
!_arnes/evidencia/visual/PRIORITY-AUDIT.md
_arnes/verify/
